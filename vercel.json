{
    "buildCommand": "npm install --registry=https://registry.npmjs.org --legacy-peer-deps && npm run build",
    "installCommand": "npm install --registry=https://registry.npmjs.org --legacy-peer-deps"
  }